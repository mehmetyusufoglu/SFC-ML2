pandas>=1.5
matplotlib>=3.5
wbgapi>=1.0
openpyxl>=3.1
# Optional: If you want to run in a headless server and still save figures
# you may also install the following to ensure reliable backends
# pillow>=9.0
